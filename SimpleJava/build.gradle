apply plugin: 'java'

sourceSets {
    main{
        java.srcDirs = ['src']
    }
}

dependencies {
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.6'
}

repositories {
    mavenCentral()
}